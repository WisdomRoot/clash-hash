#!/usr/bin/env python3
"""Bench cache support.

This module is intentionally a placeholder.

The cache behavior is specified first by `scripts/test_bench_cache.py`.
Implementation should land here, not in `scripts/bench.py`, so the planner and
cache-store logic remain testable without CLI side effects.
"""

